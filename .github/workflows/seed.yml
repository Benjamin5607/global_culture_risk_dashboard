name: 🏭 Factory Bulk Generator

on:
  workflow_dispatch: # 이 버튼을 눌러야만 실행됨 (수동 전용)

permissions:
  contents: write

jobs:
  bulk-generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Pull latest
        run: |
          git config --global user.name "Benjamin5607"
          git config --global user.email "Benjamin5607@users.noreply.github.com"
          git pull origin main --rebase || echo "Up to date"

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Requests
        run: pip install requests

      - name: Run Seed Factory
        env: 
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: python seed.py

      - name: Commit & Push
        run: |
          git add data.json
          git commit -m "🏭 Bulk Data Injection (+50 items)" || echo "No changes"
          git push https://${{ secrets.GH_PAT }}@github.com/${{ github.repository }}.git HEAD:main
