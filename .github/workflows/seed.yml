name: 🏭 Factory Bulk Generator & Deploy

on:
  workflow_dispatch: # 버튼 클릭용

# [핵심] 교통 정리: 공장 버튼을 연타해도, 옛날 작업은 취소하고 최신 작업만 배포함
concurrency:
  group: "pages"
  cancel-in-progress: true

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  bulk-generate-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    runs-on: ubuntu-latest
    
    steps:
      - name: 1. 코드 가져오기
        uses: actions/checkout@v4

      - name: 2. 최신 코드 동기화
        run: |
          git config --global user.name "Benjamin5607"
          git config --global user.email "Benjamin5607@users.noreply.github.com"
          git pull origin main --rebase || echo "Up to date"

      - name: 3. 파이썬 설치
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 4. 도구 설치
        run: pip install requests

      - name: 5. 데이터 공장 가동 (50개 생산)
        env: 
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: python seed.py

      - name: 6. 데이터 저장 및 업로드
        run: |
          git add data.json
          git commit -m "🏭 Bulk Data Injection & Deploy" || echo "No changes"
          git push origin main

      # --- 배포 섹션 ---

      - name: 7. 웹사이트 파일 묶기
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: 8. GitHub Pages에 배포하기
        id: deployment
        uses: actions/deploy-pages@v4
